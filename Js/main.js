import { updateFocus, handleSidebar } from "./utils";

document.addEventListener("DOMContentLoaded", () => {
  updateFocus();
});

document.addEventListener("DOMContentLoaded", handleSidebar);
